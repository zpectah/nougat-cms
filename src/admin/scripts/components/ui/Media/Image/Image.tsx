import React from 'react';

type ImageMediaBaseProps = {}
export type ImageMediaProps = ImageMediaBaseProps

const ImageMedia = (props: ImageMediaProps) => {
    const {} = props;
    
    return (
        <>TODO:ImageMedia</>
    );
};

export default ImageMedia;
