import React from 'react';

type VideoMediaBaseProps = {}
export type VideoMediaProps = VideoMediaBaseProps

const VideoMedia = (props: VideoMediaProps) => {
    const {} = props;

    return (
        <>TODO:VideoMedia</>
    );
};

export default VideoMedia;
