export enum Common {
    new = 'new',
}

export enum Themes {
    light = 'light',
    dark = 'dark',
}

export enum ViewTypes {
    default = 'default',
    minimal = 'minimal',
}

export enum RouteParamKeys {
    detail = 'detail',
    panel = 'panel',
    token = 'token',
}

export enum ToastContextKeys {
    info = 'info',
    success = 'success',
    error = 'error',
}
