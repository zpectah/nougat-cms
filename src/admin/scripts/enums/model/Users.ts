export enum UsersLevelKeys {
    redactor = 3,
    manager = 5,
    admin = 7,
}

export enum UsersTypeKeys {
    default = 'default',
    test = 'test',
}

export enum UsersGroupKeys {
    default = 'default',
}
