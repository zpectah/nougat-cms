import React from 'react';

type EntityDetailBaseProps = {}
export type EntityDetailProps = EntityDetailBaseProps

const EntityDetail = (props: EntityDetailProps) => {
    const {} = props;

    return (
        <>EntityDetail</>
    );
};

export default EntityDetail;
