import React from 'react';

import { ViewLayout } from '../components';

const LoginView = () => {

    return (
        <ViewLayout
            name="Login"
            variant="minimal"
            children={
                <>Login View</>
            }
        />
    );
};

export default LoginView;
