import React from 'react';

import { ViewLayout } from '../components';

const ProfileView = () => {

    return (
        <ViewLayout
            name="Profile"
            children={
                <>Profile View</>
            }
        />
    );
};

export default ProfileView;
