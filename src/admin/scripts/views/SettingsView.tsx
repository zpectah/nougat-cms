import React from 'react';

import { ViewLayout } from '../components';

const SettingsView = () => {

    return (
        <ViewLayout
            name="Settings"
            children={
                <>Settings View</>
            }
        />
    );
};

export default SettingsView;
